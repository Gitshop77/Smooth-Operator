// Cockpit version shared by the server-side discovery routes and the client
// footer. Keep this file dependency-free so it is safe to import from both
// server and client components. Bump alongside the CHANGELOG when cutting a release.

export const COCKPIT_VERSION = "0.3.1";
